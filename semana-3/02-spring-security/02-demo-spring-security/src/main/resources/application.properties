spring.datasource.url=jdbc:mysql://localhost:3306/employee_directory
spring.datasource.username=springstudent
spring.datasource.password=springstudent

server.port=8074

# llaves para JWT

rsa.private-key=classpath:certs/private.pem
rsa.public-key=classpath:certs/public.pem

jwt.ttl-seconds=3600

spring.security.oauth2.resourceserver.jwt.issuer-uri=http://localhost:8090/realms/academy

#
# Descomenta para ver que filtros de seguridad atraviesa cada request.
# Es la mejor forma de ENTENDER como las cadenas @Order eligen su camino.
#
#logging.level.org.springframework.security=DEBUG
